puts ""
puts "Why Express?"
gets()
puts "Framework sobre NodeJS, es como una extensión de NodeJS. En realidad es un módulo de NodeJS uqe nos provee de funcionalidades para escribas de una manera mucho más sencilla el servidor."
gets()
puts "Permite separar el código en módulos. Evita verbosidad, codigo testeado, no hay que reinventar la rueda. ultra fast i/o, tiene la habilidad de compartir código entre el navegador y el servidor, es decir, utilizar las mismas bibliotecas y también compartir los mismo paradigmas, etc."
gets()
puts "El buen inicio que prove Express es que ya puedes iniciar con un stack MEAN y MERN (stack muy conocidos). El stack MEAN habla de que puedes utilizar JS de inicio a fin. Es decir, MEAN reemplaza a MongoDB, Express reemplaza a Apache y Angular es un, digamos que usa una extensión de lo que podría ser una aplicación del lado cliente y NodeJS vendría a ser la N del stack MEAN."
gets()
puts "MERN es exactamente lo mismo,solamente que reemplazamos Angular por React. Utiliza JS de inicio a fin. Ahora el patrón que ves en ambos stack o el conjunto de tecnologías que permite crear aplicaciones es Express. Express esta en ambos stack."
gets()
puts "Express es fácil de aprender, ya que su obetivo es escribir código de servidor de una manera mucho más simple. También te provee de funcionalidades para integrar con bibliotecas novedosas por ejmeplo: Websockets (chat en tiempo real), WebRTC (algo tipo skyoe)"
gets()
